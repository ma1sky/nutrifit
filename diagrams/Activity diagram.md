```
@startuml
start

:Пользователь открывает приложение;
:Ввод логина и пароля;
if (Аутентификация успешна?) then (Да)
  :Пользователь вошёл в систему;
else (Нет)
  :Показать ошибку входа;
  stop
endif

:Выбор цели пользователя;
partition "Цель" {
  if (Похудение) then (Да)
    :Настройка калорий и тренировок для похудения;
  else
    if (Поддержка веса) then (Да)
      :Настройка калорий и тренировок для поддержания веса;
    else (Набор массы)
      :Настройка калорий и тренировок для набора массы;
    endif
  endif
}

:Запрос персональных рекомендаций;

partition "Прогресс" {
  :Система проверяет данные прогресса;
  :Импорт данных с внешних устройств;
  :Обновить историю прогресса;
}

partition "Рекомендации" {
  :Генерация плана питания;
  :Генерация плана тренировок;
}

partition "Отображение" {
  :Показать пользователю план питания и тренировок;

  fork
    :Пользователь редактирует план питания;
    :Обновить MealPlan;
  fork again
    :Пользователь редактирует тренировку;
    :Обновить WorkoutPlan;
  end fork
}

partition "Напоминания" {
  :Система создаёт уведомления;
  :Отправка уведомлений пользователю;
}

partition "Администрирование" {
  :Администратор входит в систему;
  :Управление пользователями и системой;
}

stop
@enduml

```